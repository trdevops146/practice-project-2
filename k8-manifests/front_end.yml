apiVersion: apps/v1
kind: Deployment
metadata:
  name: front-end-service
spec:
  replicas: 3
  selector:
    matchLabels:
      name: front-end-service
  template:
    metadata:
      labels:
        name: front-end-service
    spec:
      containers:
      - name: front-end-container
        image: trdevops/python-app:frontend-service
        env:
        - name: API_URL
          value: http://svc-api-service:5000
---
apiVersion: v1
kind: Service
metadata:
  name: svc-front-end-service
spec:
  ports:
  - port: 8000
    targetPort: 5000
    protocol: TCP
  type: NodePort
  selector:
      name: front-end-service